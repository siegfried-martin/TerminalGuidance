[gd_scene load_steps=2 format=3]

; Deliberately empty shell. All content is built in code by sandbox.gd.
; See CLAUDE.md, code-first scene policy.

[ext_resource type="Script" path="res://scripts/sandbox/sandbox.gd" id="1"]

[node name="Sandbox" type="Node3D"]
script = ExtResource("1")

[gd_scene load_steps=2 format=3]

; Deliberately empty shell. All content is built in code by combat_arena.gd.
; See CLAUDE.md, code-first scene policy, and ADR 0027.

[ext_resource type="Script" path="res://scripts/arena/combat_arena.gd" id="1"]

[node name="CombatArena" type="Node3D"]
script = ExtResource("1")
